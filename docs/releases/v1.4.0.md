> 2026-06-25 · `repo-visibility` → `main`

## Summary

Adds a new web-based terminal app — a browser console that runs and previews the template's apps live — built on the same architecture as the other reference apps.

---

## Features

- A new web terminal app: an in-browser console that connects to a local backend, runs any of the template's apps, streams their output live, and shows a side-by-side preview of the running app
- Start and stop apps from a toolbar and follow their terminal output in real time, including resize and scroll handling for a real terminal feel
- A companion local backend that launches apps, manages their sessions, and serves the preview, with basic safeguards around what it will run

## Agent Context Improvements

- New forbidden-pattern rules that keep enums lean by moving their helpers and conversions into extensions, and that discourage comments which merely restate what the code already says
- Guidance on keeping every AI agent's instructions in sync across all supported tools whenever a shared rule changes
- Architecture and convention docs extended to cover the new app and its real-time, socket-based patterns
