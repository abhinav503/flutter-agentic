<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ runtime notification permission; firebase_messaging's
         requestPermission() is what raises the prompt. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Edit Profile's "take a photo" avatar option. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- No READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE on purpose. The only
         gallery read in the app is Edit Profile's avatar picker, which runs
         through the Android Photo Picker (ImagePickerService opts in via
         `useAndroidPhotoPicker`). The picker hands back a single URI the user
         chose, so it needs no storage permission at all — and declaring one
         anyway trips Google Play's Photo and Video Permissions policy, which
         reserves it for apps with a frequent need. -->
    <!-- Address form's "use my location" prefill (geolocator). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:label="CordeliaApps"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <!-- Channel a *backgrounded* push is drawn on. Must match
             LocalNotificationService._channelId, which creates it. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel"/>
        <!-- A backgrounded push is drawn by the OS, which never runs our Dart
             — without these it falls back to the launcher icon and renders
             the same donut the foreground path used to. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Help & Support hands a mailto:, a tel: and an https: to the
             device. Android 11+ hides packages that aren't declared here, so
             without these the launch reports failure on a phone that does in
             fact have a mail client — and the screen falls back to copying
             an address nobody needed copied. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
