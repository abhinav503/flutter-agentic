{
  "_doc": "The 'dailymart' storefront template's own theme. Placeholder: selects the shared 'rocketWarm' preset (packages/core/lib/core/theme/app_theme_presets.dart) until dailymart's real design pack is sampled — visibly distinct from gravia's emerald so the per-store template/theme swap is provable end-to-end. Loaded at runtime by StorefrontPage once a store's templateId resolves to 'dailymart', not at boot.",
  "activeTheme": "rocketWarm"
}
