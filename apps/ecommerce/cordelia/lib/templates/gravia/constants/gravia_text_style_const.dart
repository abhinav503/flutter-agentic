import 'package:cordelia/constants/cordelia_text_style_const.dart';

export 'package:cordelia/constants/cordelia_text_style_const.dart';

/// The gravia kit's type scale. The values live app-level as
/// [CordeliaTextStyleConst] — the app's own chrome (auth, discovery) was
/// designed on the same scale — and this alias keeps the pack's call sites
/// reading as pack tokens.
typedef GraviaTextStyleConst = CordeliaTextStyleConst;
